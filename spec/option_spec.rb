require('spec_helper')

describe(Option) do
  # it('validates presence of answer') do
  #     option = Option.create({:answer => ''})
  #   expect(option.save()).to(eq(false))
  # end
  #
  # describe('#question') do
  #   it("tells which question an option belongs to") do
  #     survey = Survey.create({:name => 'kitties'})
  #     question = Question.create({:description => 'do you like kitties', :survey_id => survey.id})
  #     option = Option.create({:answer => 'Yup', :question_id => question.id})
  #   expect(option.question.id()).to(eq(question.id))
  #   end
  # end


end
